Before marking a coding task done: (1) Build frontend and server locally (npm run build:full) and ensure no TypeScript build errors. (2) Run dev servers (npm run dev:full) and verify key endpoints: /health returns OK; /api/test responds; frontend loads at / with login redirect. (3) If DB schema changed, run npx prisma generate and db push/migrate; seed if needed (npm run db:reset). (4) For API or route changes, validate via test scripts (test-api.js, tests/deployment-validation.js where applicable). (5) Keep VITE_API_URL=/api for integrated proxy. (6) Check Docker build locally if deployment-related (docker build, docker-compose.prod.yml up). (7) Review CORS, JWT_SECRET in prod. (8) Update docs if interfaces or endpoints changed.