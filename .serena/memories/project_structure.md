Key directories: src/components (UI + ui subfolder), src/pages (route pages), src/contexts (AuthContext), src/hooks, src/lib (frontend utils + API client + risk/analysis helpers + rag), src/server (Express app with routes, middleware, services, prisma client under lib, index.ts for richer dev server and simple-index.ts for prod/static), prisma (schema.prisma, dev.db, seed scripts), scripts (utility scripts + Windows .bat to restart dev), deployment and azure folders for deployment automation, docker-compose.dev.yml and docker-compose.prod.yml, Dockerfile and Dockerfile.dev. Build outputs: dist (frontend), dist-server (server).